import { NgModule } from '@angular/core';

// Simple module without Firebase dependencies
// Use this temporarily until Firebase dependencies are fixed

@NgModule({
    providers: [
        // No Firebase providers - using mock services instead
    ]
})
export class FirebaseModule {}