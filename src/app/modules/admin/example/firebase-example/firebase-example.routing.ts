import { Route } from '@angular/router';
import { FirebaseExampleComponent } from './firebase-example.component';

export const firebaseExampleRoutes: Route[] = [
    {
        path: '',
        component: FirebaseExampleComponent
    }
];