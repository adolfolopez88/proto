<div class="flex flex-col flex-auto min-w-0">
    <!-- Header -->
    <div class="flex flex-col sm:flex-row flex-0 sm:items-center sm:justify-between p-6 sm:py-8 sm:px-10 border-b bg-card dark:bg-transparent">
        <div class="flex-1 min-w-0">
            <!-- Breadcrumbs -->
            <div class="flex flex-wrap items-center font-medium">
                <div>
                    <a class="whitespace-nowrap text-primary-500">Inicio</a>
                </div>
                <div class="flex items-center ml-1 whitespace-nowrap">
                    <mat-icon
                        class="icon-size-5 text-secondary"
                        [svgIcon]="'heroicons_mini:chevron-right'"></mat-icon>
                    <a class="ml-1 text-primary-500">Firebase Messaging</a>
                </div>
            </div>
            <!-- Title -->
            <div class="mt-2">
                <h2 class="text-3xl md:text-4xl font-extrabold tracking-tight leading-7 sm:leading-10 truncate">
                    Firebase Cloud Messaging Demo
                </h2>
            </div>
        </div>
    </div>

    <!-- Main -->
    <div class="flex-auto p-6 sm:p-10">
        <div class="max-w-6xl mx-auto">
            <!-- Estado del Servicio -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                <!-- Card de Estado de Permisos -->
                <div class="flex flex-col bg-card shadow rounded-2xl overflow-hidden">
                    <div class="flex items-center justify-between p-6 bg-gray-50 dark:bg-gray-700">
                        <h3 class="text-lg font-semibold">Estado de Permisos</h3>
                        <mat-icon [svgIcon]="'heroicons_outline:shield-check'" class="text-blue-600"></mat-icon>
                    </div>
                    <div class="p-6">
                        <div class="flex items-center justify-between mb-4">
                            <span class="text-sm font-medium">Notificaciones:</span>
                            <span [class]="getPermissionClass()" class="text-sm font-semibold">
                                {{ getPermissionText() }}
                            </span>
                        </div>
                        <button
                            mat-flat-button
                            color="primary"
                            [disabled]="notificationPermission === 'granted'"
                            (click)="requestPermission()"
                            class="w-full">
                            <mat-icon [svgIcon]="'heroicons_outline:bell'"></mat-icon>
                            <span class="ml-2">Solicitar Permisos</span>
                        </button>
                    </div>
                </div>

                <!-- Card de Token -->
                <div class="flex flex-col bg-card shadow rounded-2xl overflow-hidden">
                    <div class="flex items-center justify-between p-6 bg-gray-50 dark:bg-gray-700">
                        <h3 class="text-lg font-semibold">Token FCM</h3>
                        <mat-icon [svgIcon]="'heroicons_outline:key'" class="text-green-600"></mat-icon>
                    </div>
                    <div class="p-6">
                        <div class="mb-4">
                            <textarea
                                class="w-full p-3 text-xs border rounded-lg bg-gray-50 dark:bg-gray-700 font-mono"
                                rows="3"
                                readonly
                                [value]="currentToken || 'No hay token disponible'">
                            </textarea>
                        </div>
                        <button
                            mat-flat-button
                            color="accent"
                            [disabled]="!currentToken"
                            (click)="copyTokenToClipboard()"
                            class="w-full">
                            <mat-icon [svgIcon]="'heroicons_outline:clipboard-document'"></mat-icon>
                            <span class="ml-2">Copiar Token</span>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Controles de Testing -->
            <div class="bg-card shadow rounded-2xl overflow-hidden mb-8">
                <div class="flex items-center justify-between p-6 bg-blue-50 dark:bg-blue-900">
                    <h3 class="text-lg font-semibold">Controles de Testing</h3>
                    <mat-icon [svgIcon]="'heroicons_outline:cog-6-tooth'" class="text-blue-600"></mat-icon>
                </div>
                <div class="p-6">
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                        <button
                            mat-stroked-button
                            color="primary"
                            (click)="simulateNotification()">
                            <mat-icon [svgIcon]="'heroicons_outline:bell'"></mat-icon>
                            <span class="ml-2">Simular Notificación</span>
                        </button>
                        <button
                            mat-stroked-button
                            color="warn"
                            [disabled]="messages.length === 0"
                            (click)="clearAllMessages()">
                            <mat-icon [svgIcon]="'heroicons_outline:trash'"></mat-icon>
                            <span class="ml-2">Limpiar Todo</span>
                        </button>
                        <button
                            mat-stroked-button
                            color="accent"
                            (click)="requestPermission()">
                            <mat-icon [svgIcon]="'heroicons_outline:arrow-path'"></mat-icon>
                            <span class="ml-2">Refrescar Token</span>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Lista de Mensajes -->
            <div class="bg-card shadow rounded-2xl overflow-hidden">
                <div class="flex items-center justify-between p-6 bg-gray-50 dark:bg-gray-700">
                    <h3 class="text-lg font-semibold">
                        Mensajes Recibidos
                        <span *ngIf="unreadCount > 0" class="ml-2 px-2 py-1 text-xs bg-red-500 text-white rounded-full">
                            {{ unreadCount }}
                        </span>
                    </h3>
                    <mat-icon [svgIcon]="'heroicons_outline:inbox'" class="text-purple-600"></mat-icon>
                </div>
                <div class="max-h-96 overflow-y-auto">
                    <!-- Estado vacío -->
                    <div *ngIf="messages.length === 0" class="flex flex-col items-center justify-center p-12 text-gray-500">
                        <mat-icon [svgIcon]="'heroicons_outline:inbox'" class="text-6xl mb-4 opacity-50"></mat-icon>
                        <h4 class="text-lg font-medium mb-2">No hay mensajes</h4>
                        <p class="text-sm text-center">Los mensajes de Firebase Cloud Messaging aparecerán aquí</p>
                    </div>

                    <!-- Lista de mensajes -->
                    <div *ngFor="let message of messages; trackBy: trackByMessageId"
                         class="border-b border-gray-100 dark:border-gray-700 last:border-b-0">
                        <div class="p-6 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors"
                             [class.bg-blue-50]="message.read === false"
                             [class.dark:bg-blue-900/20]="message.read === false">
                            
                            <div class="flex items-start space-x-4">
                                <!-- Icono -->
                                <div class="flex-shrink-0">
                                    <div *ngIf="message.icon; else defaultIcon">
                                        <img [src]="message.icon" alt="Notification" class="w-10 h-10 rounded-full">
                                    </div>
                                    <ng-template #defaultIcon>
                                        <div class="w-10 h-10 bg-blue-100 dark:bg-blue-900 rounded-full flex items-center justify-center">
                                            <mat-icon class="text-blue-600 dark:text-blue-400" 
                                                      [svgIcon]="'heroicons_outline:bell'"></mat-icon>
                                        </div>
                                    </ng-template>
                                </div>

                                <!-- Contenido del mensaje -->
                                <div class="flex-1 min-w-0">
                                    <div class="flex items-start justify-between">
                                        <div class="flex-1">
                                            <h4 *ngIf="message.title" class="text-sm font-semibold text-gray-900 dark:text-white">
                                                {{ message.title }}
                                            </h4>
                                            <p *ngIf="message.body" class="text-sm text-gray-600 dark:text-gray-300 mt-1">
                                                {{ message.body }}
                                            </p>
                                            <div class="flex items-center mt-3 space-x-4">
                                                <span class="text-xs text-gray-400">
                                                    {{ getRelativeTime(message.timestamp) }}
                                                </span>
                                                <span *ngIf="message.read === false" class="px-2 py-1 text-xs bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 rounded-full">
                                                    Nuevo
                                                </span>
                                            </div>
                                        </div>
                                        
                                        <!-- Acciones -->
                                        <div class="flex items-center space-x-2 ml-4">
                                            <button
                                                *ngIf="message.read === false"
                                                mat-icon-button
                                                (click)="markAsRead(message.id)"
                                                class="text-blue-600 hover:text-blue-800"
                                                title="Marcar como leído">
                                                <mat-icon [svgIcon]="'heroicons_outline:check'"></mat-icon>
                                            </button>
                                            <button
                                                mat-icon-button
                                                (click)="deleteMessage(message.id)"
                                                class="text-red-600 hover:text-red-800"
                                                title="Eliminar">
                                                <mat-icon [svgIcon]="'heroicons_outline:trash'"></mat-icon>
                                            </button>
                                        </div>
                                    </div>

                                    <!-- Datos adicionales -->
                                    <div *ngIf="message.data" class="mt-3 p-3 bg-gray-100 dark:bg-gray-800 rounded-lg">
                                        <p class="text-xs font-medium text-gray-600 dark:text-gray-400 mb-1">Datos adicionales:</p>
                                        <pre class="text-xs text-gray-700 dark:text-gray-300 font-mono">{{ message.data | json }}</pre>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Información adicional -->
            <div class="mt-8 p-6 bg-yellow-50 dark:bg-yellow-900/20 rounded-2xl">
                <div class="flex items-start space-x-3">
                    <mat-icon [svgIcon]="'heroicons_outline:information-circle'" class="text-yellow-600 flex-shrink-0 mt-0.5"></mat-icon>
                    <div>
                        <h4 class="text-sm font-semibold text-yellow-800 dark:text-yellow-200 mb-2">
                            Instrucciones para testing
                        </h4>
                        <ul class="text-sm text-yellow-700 dark:text-yellow-300 space-y-1">
                            <li>• Copia el token FCM y úsalo en tu consola de Firebase para enviar notificaciones de prueba</li>
                            <li>• Los mensajes se recibirán tanto en primer plano como en segundo plano</li>
                            <li>• Puedes simular notificaciones localmente con el botón "Simular Notificación"</li>
                            <li>• Las notificaciones se almacenan localmente hasta que las elimines</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>